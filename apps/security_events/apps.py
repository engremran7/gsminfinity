from django.apps import AppConfig


class SecurityEventsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.security_events"
    verbose_name = "Security Events"
